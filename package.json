{
  "name": "alpha",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "typecheck": "bun run --filter '*' typecheck",
    "test": "bun run --filter '*' test",
    "lint": "bunx biome check ."
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
